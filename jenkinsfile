pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Running Jenkins pipeline for DEV branch'
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests for DEV branch'
            }
        }
    }
}
